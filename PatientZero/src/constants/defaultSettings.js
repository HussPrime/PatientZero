// Purpose: Centralizes the default values used to create and display a population.
export const DEFAULT_SETTINGS = {
  populationSize: 320,
  initialInfected: 3,
  simulationWidth: 900,
  simulationHeight: 520,
  minInitialSpeed: -1,
  maxInitialSpeed: 1,
  // TODO: Add infectionDuration, transmissionRate, recoveryRate, and infectionRadius here when the pure model is connected.
};

